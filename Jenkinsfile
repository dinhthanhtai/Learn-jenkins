pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'docker --version'
            }
        }
        stage('Build') {
            steps {
                echo 'DKM Jenkins'
            }
        }
    }
}
